# Structure Density Harness Contract

The replacement harness must generate the exact rectangular stage, flush and stop cleanly, decode every target Anvil slot offline, reject unreadable or coordinate-shifted slots, rerun only from explicit checkpoints, and hash raw regions, logs, run manifests, and analyses.

A pregenerator completion message alone is never acceptance evidence.

