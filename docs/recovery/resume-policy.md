# Density Resume Policy

Resume only from the last accepted checkpoint. Verify its canonical JSON hash before and after the resumed attempt. Preserve interrupted worlds under unique names, never reinterpret partial output as complete, and rerun the exact missing target rectangle until every target slot passes.

