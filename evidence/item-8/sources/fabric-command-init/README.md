# fabric-command-init source roles

Extractor 2e74941. Independent r1 reproduction matches identities.json and every
disassembly byte for byte. Manifest SHA-256:
54cc77ba015cd890542609a6a8a98742763b3bde85eef35c857ad253445205a4.

```sh
uv run -m tools.inspect_item8_pool_elements --archive forgified-fabric-api-0.116.7+2.2.4+1.21.1.jar --nested-archive META-INF/jars/fabric-command-api-v2-2.2.28+36d727be19.jar --class-name org/sinytra/fabric/command_api/FabricCommandApiV2.class --output evidence/raw/item8/fabric-command-init-r1
```

The initializer registers caller-supplied command argument types from initially empty maps and forwards command registration events to consumers. Together with the existing selector-flag hook, it supplies command support, not an independent generated site.

Reuse the corresponding entry capture. Client initializers are guarded by
Dist.isClient. These source roles do not prove effective consumer behavior.
