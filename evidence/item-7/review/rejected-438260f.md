# Item 7 rejected exact-SHA review

Reviewed revision: `438260f40fd0d50ff5f087a2b8aac028d5a39927`

Verdict: `REJECTED`

The goal and constraint lane passed this revision. The runtime debugging audit also passed the then-existing surface. The aggregate review still failed for two independent blockers:

1. The code-quality lane proved that staging pinned its sources but still created, wrote, and published the destination tree through replaceable pathnames. It also proved that two source-swap regressions patched obsolete `shutil` calls, so those tests passed without triggering their declared mutations.
2. The context and evidence lane proved that the immutable r2 release was bound to revision `b13344e8eaa39528b61643bf24534d709cfff131`, before the accepted custody fixes. Re-downloading r2 could prove its bytes, but could not prove that the final fail-closed procedure produced those bytes.

The hands-on QA result from this review is not accepted as exact-SHA coverage. It observed shared uncommitted changes and reported 164 focused tests even though revision `438260f40fd0d50ff5f087a2b8aac028d5a39927` contained 162. The security lane did not return a verdict because its harness refused the requested task. Neither result overrides the two reproduced blockers.

Disposition:

- `f57503d` makes output-tree creation, writing, identity checks, and no-replace publication descriptor-bound.
- The world and core source-swap tests now intercept the active copy seam and assert that the mutation actually occurred.
- `4503d64` tightens the release-verifier fixture and proves the repository-bound verifier does not consult local Git.
- The final staging implementation rejected the original hardlinked raw roots. Fresh r3 stages were therefore constructed from the preserved r2 single-link snapshots, whose file inventories and bytes match the earlier manifests.
- Four r3 archives were generated by the final implementation, restored into absent targets, published under `item-7-raw-evidence-2026-09-04-r3`, independently downloaded, and verified. Their sizes and SHA-256 values are byte-identical to r2.
- `7bcce66` commits the r3 manifests, restore receipts, publication receipt, and rebuilt completion receipt.

This rejected revision is not approval. Fresh independent review must bind the complete final candidate SHA.
